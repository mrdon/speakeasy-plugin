$forkerFullName ($forker) has forked your Speakeasy extension '$plugin.name' with the following description:

$forkedPlugin.description

#if (!$otherForkedExtensions.isEmpty())

$forkerFullName has also forked:

#foreach ($ext in $otherForkedExtensions)

 * $ext.name by $userResolver.getFullName($ext.author)
#end
#end
